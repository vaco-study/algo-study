function solution(s) {
  let answer = 0;
  let minimum = Number.MIN_SAFE_INTEGER;
  let count = 0;

  for (let i = 1; i < s.length; i++) {
    let temporaryString = s.substr(0, i);
  }

  return answer;
}
