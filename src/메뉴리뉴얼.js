function solution(orders, course) {
  var answer = [];
  const orderFreqCounter = {};

  // 단품메뉴의 갯수에 맞는 각 손님주문의 조합을 찾는다.
  // 손님별로 만들수 있는 조합을 만든다.
  // 조합 전체의 배열로 freqCounter만든다.
  // 해당 조합에서 가장 많은 빈도의 조합을 찾는다.해당 조합을 결과에 담는다.
  // 이것들을 코스별로 반복한다.
  // 결과를 오름차순 정렬해서 돌려준다.

  return answer;
}
