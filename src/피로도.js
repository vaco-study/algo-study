const solution = (fatigue, dungeons) => {
  let clearCount = 0;
  let dungeonsLength = dungeons.length;
  const visitedDungeons = newArray(dungeonsLength).fill(false);

  console.log(visitedDungeons);

  return answer;
};
